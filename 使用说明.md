# 人事工作台 · 使用说明（v1.1 一期完整版）

绿色免安装单机版。拷贝整个目录即可使用，删除目录即卸载，数据不残留系统。

## 一、启动

1. 双击 **`启动工作台.bat`**
2. 等待 2~5 秒，浏览器自动打开 `http://127.0.0.1:5270`
3. 关闭黑色命令窗口即退出（数据已自动保存）
4. 若 5270 被占用，系统自动改用 5271、5272…（窗口会显示实际地址）
5. **页面右上角有「查看使用帮助」**，点开即本篇的图文版（`使用说明-专员版.html`，新标签页打开）

> 不依赖网络、不依赖公司系统、不装任何数据库。首次启动自动建库并预置 42 条常用回复 + 10 个文书模板。

## 二、十二个模块

| 模块 | 入口 | 一句话说明 |
|---|---|---|
| 待办中心 | 首页 | 全部提醒聚合，一键完成 |
| 员工档案 | 左侧「员工档案」 | Excel 导入 / 手动维护 / 异动时间轴，敏感字段掩码 |
| 简历管理 | 「招聘」分组 | 批量导入 → 自动解析 → 三档初筛（简历解析无需大模型） |
| 合同台账 | 「合同」分组 | Excel 导入 + 到期/第2次合同/试用期自动提醒 |
| 考勤异常 | 「考勤」分组 | Excel 导入 → 分组 → 批量处理 → 月度汇总 |
| 假期计算 | 「考勤」分组 | 司龄 → 年假/婚假/产假额度计算 |
| 工资答疑 | 「薪酬」分组 | 工资表导入 → 按人按月查明细 → 差异原因参考 |
| 面试安排 | 「员工关系」分组 | 面试 CRUD + 状态机 + 提醒 |
| 入离职清单 | 「员工关系」分组 | 发起清单 → 逐项勾选 → 进度条 |
| 制度知识库 | 「知识工具」分组 | 上传切片 + 条款级检索带出处 |
| 常用回复 | 「知识工具」分组 | 42 条话术 + 一键复制 + /nj 快捷编号 |
| 文书生成 | 「知识工具」分组 | 10 个模板（JD/通知/offer/证明）+ 变量填充 + 员工带入 |

## 三、各模块要点

### 1. 员工档案（新）
- Excel 导入（模板：工号/姓名/性别/出生日期/手机/邮箱/部门/岗位/入职日期/试用期到期/状态/学历/身份证/银行卡）
- 详情页：身份证/银行卡**默认掩码**显示（明文存储于本机，注意保管 data/ 目录）
- 异动时间轴：入职/转正/调岗/调薪/离职逐条记录

### 2. 考勤异常批处理（新）
- 模板列：姓名/月份/日期/异常类型/原因 → 导入后按月份筛选
- 勾选多行 → 批量置「已确认/已处理」+ 处理备注
- 顶部统计卡显示待确认数、类型分布

### 3. 假期额度计算器（新）
- 选员工自动带司龄（或手填社会工龄）→ 按《职工带薪年休假条例》计算年假
- 婚假 3 天、产假 98 天参考值

### 4. 文书生成器（新）
- 预置 10 模板：JD×2、通知×4（转正/调薪/试用期延长/会议）、offer、离职证明、在职证明、欢迎信
- 选模板 → 选员工自动带入姓名/部门/岗位/入职日期 → 填变量 → 预览 → 一键复制
- 可新建/编辑模板，支持任意 `{{变量}}`

### 5. 工资答疑助手（新）
- 模板列：月份/姓名/基本工资/绩效/补贴/加班费/社保/公积金/个税/其他扣款/实发
- 按 月份+员工 筛选明细；内置「工资少了」6 类常见原因话术参考

### 6. 入离职清单（新）
- 发起（内置入职 13 项 / 离职 10 项模板，可自定义）
- 逐项勾选自动存进度，进度条百分比展示

### 7. 合同台账与到期提醒

- 首次使用：进「合同台账」→「Excel 批量导入」→ 下载模板 → 把现有台账粘贴进去 → 上传
- 系统自动提醒（规则可改 `配置.yaml`）：
  - 合同到期前 60 / 30 / 7 天生成待办
  - **第 2 次及以上合同**到期前 90 天特别预警「下次应为无固定期限」（劳动合同法第 14 条）
  - 试用期到期前 15 / 7 天提醒转正评估
- 所有提醒汇总到「待办中心」，处理完点「完成」

### 8. 常用回复（最高频功能，无 AI 依赖）

- 「常用回复」→ 搜索（支持 /nj 快捷编号）→ 点「复制」→ 粘贴到微信/钉钉直接发送
- 支持变量：`{{姓名}} {{司龄}} {{年假总额}}` 等，复制前可手动替换成实际数字
- 用得多排得前；「新建回复」可随时增补，回答过的新问题建议随手存一条

### 9. 制度知识库

- 「制度知识库」→ 上传 txt / md / docx / 文字版 pdf
- 自动按「第X条」切片入库，检索结果带出处（文档 + 条款 + 版本）
- 无 AI 模式：返回命中条款原文（关键词命中）；后续接入大模型后可自然语言问答
- 扫描件 PDF 一期不支持，请先用 Word/OCR 转文字版

### 10. 面试安排与跟进

- 录入候选人 / 岗位 / 面试官 / 时间 / 方式，状态随时切换
- 到期提醒进入待办中心（无邮件提醒，一期仅界面提醒）
- 面试官评价：系统暂无在线评价，可口头收集后代录在备注中

### 11. 简历管理与初筛

- **导入**：「简历管理」→「批量导入简历」→ 选文件（支持多选 / ZIP 自动解压，pdf/docx/txt）
- **解析**：无需大模型。文字版 PDF 毫秒级提取；扫描件自动识别并标记，需人工看原文
- **去重**：同文件自动跳过；同姓名+手机号归并为一人
- **初筛**：新建岗位（硬条件：学历/年限/城市/薪资上限/技能）→ 上传时归入岗位 → 设软条件权重 → 「应用条件并筛选」
- **三档结果**：通过（≥80）/ 待定（60-79）/ 不通过（<60），每条理由可展开；硬条件不满足直接归不通过档
- **合规**：性别/婚育/户籍不作为筛选项；年龄用「工作年限」替代
- **修正**：详情页黄色字段为低置信度，直接改后保存

## 四、数据与备份

| 事项 | 说明 |
|---|---|
| 数据位置 | 全部在 `data/` 目录（hr.db + uploads + backups） |
| 备份 | 拷贝 `data/` 目录即可（建议拷贝前关闭程序） |
| 自动备份 | 每日首次启动自动备份 hr.db 到 `data/backups/`，保留最近 7 份 |
| 恢复 | 关闭程序 → 用备份覆盖 `data/hr.db` → 重启 |

## 五、安全与访问口令

- **默认仅本机访问**（`host: 127.0.0.1`），不对外暴露。
- **访问口令（可选）**：在 `配置.yaml` 的 `server.access_password` 填入口令 → 重启后需登录才能进入。留空则无登录。
- **局域网访问**：把 `host` 改成 `0.0.0.0` 时，**必须**设置 `access_password`，否则程序拒绝启动（防止身份证/薪资等敏感数据在局域网裸奔）。
- **敏感字段**：员工身份证/银行卡在页面默认掩码显示；编辑时留空即不改动（要改则重输完整值）。

## 六、常见问题

| 问题 | 处理 |
|---|---|
| 启动后没自动开浏览器 | 手动打开窗口里显示的地址 |
| 端口冲突 | 自动 +1 直到可用；也可改 `配置.yaml` 的 port |
| 上传 PDF 提示"未提取到文本" | 该 PDF 是扫描件（图片），需先转文字版 |
| Excel 导入报错 | 确认表头与模板一致，日期列格式为 2026-08-31 |
| 换电脑 | 拷贝整个目录（含 data/）到新电脑双击启动即可 |

## 七、二期预告（本次未做）

- 网盘资料批量清洗（制度/合同/表单 ETL）
- 经理驾驶舱（人力看板 / 风险预警 / 一键出报告）
- 大模型接入（本地 Ollama 或云 API，三级降级）
- 简历语义匹配（LLM 兜底低置信度字段）
